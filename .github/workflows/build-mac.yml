name: Build macOS Desktop Agent DMG

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build-mac:
    runs-on: macos-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install Dependencies & Build DMG
        run: |
          cd desktop-agent
          npm install
          npm run dist:mac

      - name: Upload macOS DMG Artifact
        uses: actions/upload-artifact@v4
        with:
          name: Improx-Agent-Setup-macOS-DMG
          path: desktop-agent/release/*.dmg